# MATS 소개 사이트 — 관리 가이드

이 저장소는 **MATS 소개 페이지** 하나만 담고 있습니다. 배포는 Vercel이 `homepage/` 를
사이트 루트로 서빙합니다(`https://ats-home.vercel.app`).

> **[2026-09-01] ATS → MATS 로 내용을 교체했습니다.**
> 종전 페이지는 은퇴한 **ATS2.0**(키움 Open API+ COM 기반, `ATS_Setup.exe`)을 안내하고
> 설치본을 직접 배포하고 있었습니다. 현재 운영 중인 시스템은 **MATS**(키움 REST 기반,
> 폰 앱·웹 대시보드)이므로 소개 대상을 바꾸고, 공개 다운로드 링크는 **내렸습니다**.

---

## 1. 구조

```
homepage/
  index.html     소개 페이지 본문 (섹션: 무엇인가 · 동작 방식 · 안전장치 · 시작하기 · 문의 · 고지)
  index.css      밝은 테마. 색은 :root 의 CSS 변수만 고치면 전체가 따라옵니다
  index.js       상단 바 · 등장 애니메이션 · 앵커 이동 (+ 옛 서비스 워커 제거용 등록)
  sw.js          ★기능 없음. 과거에 설치된 서비스 워커를 지우는 묘비 파일입니다
  manifest.json  최소 정보만
```

### 그림은 **이미지 파일이 아니라 인라인 SVG** 입니다
실제 화면 캡처가 없는 상태에서 그럴듯한 '가짜 스크린샷'을 만들면 실물과 달라져 오해를 부르고,
나중에 캡처가 생기면 통째로 갈아야 합니다. 그래서 정보를 담은 **도해 3종**(구성도 / 하루 흐름 /
청산 도해)을 SVG 로 그려 넣었습니다. 파일이 안 늘고, 확대해도 안 깨지고, 색이 테마 변수를
그대로 따라갑니다.

화면에 들어올 때 움직입니다(선이 그려지고 · 표식이 순서대로 켜지고 · 연결선 위로 점이 흐름).
기존 `IntersectionObserver` 가 `.reveal` 에 `.active` 를 붙이는 것을 그대로 씁니다 —
페이지를 열자마자 아래쪽 도해가 다 돌아버리면 정작 볼 때는 끝나 있기 때문입니다.
⚠️`prefers-reduced-motion` 에서는 애니메이션을 끄되 **최종 상태로 보이게** 해 두었습니다.
끄기만 하면 `opacity:0` 과 `stroke-dashoffset` 이 남아 도해가 통째로 사라집니다.

### sw.js 를 지우지 마세요
종전 서비스 워커는 `/homepage/...` 경로를 캐시하려다 설치에 실패해 실제로는 동작한 적이
없습니다. 다만 과거 배포에서 설치된 워커가 남아 있을 수 있고, 그 워커는 **캐시 우선 + 정리
없음** 이라 한 번 받아 간 방문자에게 옛 페이지를 영구히 보여줍니다. 지금의 `sw.js` 는 그것을
등록 해제하고 캐시를 지우는 역할만 합니다. 파일을 삭제하면 옛 워커가 그대로 살아남습니다.

---

## 2. 고칠 때 주의할 것

- **전략 수치를 페이지에 적지 마세요.** 손절·익절·트레일링 폭 같은 값은 스윕으로 바뀝니다.
  숫자를 여기에 박으면 값이 바뀐 뒤에도 페이지만 옛 숫자를 보여주게 됩니다(실제로 앱의
  '작동 방식' 화면에서 같은 사고가 두 번 났습니다). 페이지는 **"앱의 작동 방식 화면에서
  현재 값을 확인"** 으로 넘깁니다.
- **수익을 암시하는 문구를 쓰지 마세요.** '승률 높은', '정밀 타격' 같은 표현은 뺐습니다.
- **고지 섹션(`#disclaimer`)을 지우지 마세요.** 투자자문·일임이 아니라는 점, 원금 손실
  가능성, 기술적 실패 가능성을 담고 있습니다.
- 연락처는 아직 비어 있습니다. `#contact` 섹션의 TODO 주석 자리에 채우면 됩니다.
  이메일 주소를 그대로 노출하면 수집 봇의 대상이 되므로 문의 폼을 권합니다.

---

## 3. 배포

`main` 에 푸시하면 Vercel이 자동 배포합니다.

```powershell
git status
git add .
git commit -m "변경 내용"
git push origin main
```

> PowerShell에서는 `&&` 가 동작하지 않습니다. 한 줄씩 실행하세요.

배포 후 확인할 것:
- 페이지가 뜨는지 (`https://ats-home.vercel.app`)
- 상단 메뉴 5개 앵커가 모두 해당 섹션으로 가는지
- 휴대폰 폭에서 글자가 넘치지 않는지
